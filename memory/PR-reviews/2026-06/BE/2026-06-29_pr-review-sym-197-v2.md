# PR Review — SYM-197 (BE) — Re-review

## Review opened
- **Date:** 2026-06-29
- **Repo:** BE
- **Branch:** feature/sym-197
- **Base branch:** dev
- **Issue:** SYM-197
- **Note:** This is a re-review. First review was 2026-06-25 (`2026-06-25_pr-review-sym-197.md`).

## Dev's stated focus
Foundation work: adds `linked_*_id` columns to the users table (linking a SA user to their RM entity — Owner/Tenant/Vendor/VendorEmployee) and the corresponding RM Role ID config keys.

## Status of previous findings
- **Blocker 1 (base migration):** NOT addressed. Git log shows it was actively reversed ("removing column migration, adding base table col"). Still present.
- **Nit 2 (RM_SA_VENDOR_EMP_ROLE_ID comment):** NOT addressed.
- **Nit 3 (trailing newline):** NOT addressed.
- **Nit 4 (branch naming):** Not applicable (branch name won't change mid-PR).

## Findings

### Blockers

#### 1. Base migration still modified — previous blocker NOT fixed
- **File + line:** `database/migrations/0001_01_01_000000_create_users_table.php:29-32`
- **Severity:** blocker
- **What's wrong:** The 4 `linked_*_id` columns are still added to the `0001` base migration. Commit "removing column migration, adding base table col" confirms this was intentionally reversed after the first review.
- **Why:** RULES.md rule: "Never add new columns to a base migration — any environment where the table already exists will not re-run it." Staging/prod/existing dev machines will silently be missing these columns.
- **Suggested fix:** Create `add_linked_entity_ids_to_users_table` migration via `php artisan make:migration`. Move the 4 column definitions there.
- **Comment for the dev:** "The base migration blocker from the first review is still here — and the git log shows it was reversed after the review. This will silently break every existing environment. Please create a new migration for these 4 columns. `0001` is only correct for a brand-new install; staging and prod already have the `users` table and won't re-run this. The standalone migration you had originally was the right approach."

#### 2. AWS env vars accidentally deleted from `.env.example`
- **File + line:** `.env.example` (lines 69-75 in dev, removed in this branch)
- **Severity:** blocker
- **What's wrong:** "merge conflicts" commit removed `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_DEFAULT_REGION`, `AWS_BUCKET`, `AWS_USE_PATH_STYLE_ENDPOINT`. These are actively used in `config/filesystems.php:52-55`, `config/services.php:26-28`, `config/cache.php:83-85`, `config/queue.php:58-63`.
- **Why:** New environments built from this `.env.example` will be missing required configuration keys. This is a setup document regression from bad conflict resolution.
- **Suggested fix:** Restore the AWS block from `dev`. `VITE_APP_NAME` can stay removed (no PHP callers).
- **Comment for the dev:** "The 'merge conflicts' commit accidentally dropped the `AWS_*` block from `.env.example`. Those vars are still used in `config/filesystems.php`, `config/services.php`, `config/cache.php`, and `config/queue.php` — any new environment built from this file will be missing them. Please restore that block. `VITE_APP_NAME` is fine to leave out since there's no Vite usage in the BE."

---

### Nits

#### 3. `RM_SA_VENDOR_EMP_ROLE_ID=` still has no explanatory comment
- **File + line:** `.env.example:88`
- **Severity:** nit
- **What's wrong:** Empty value with no inline comment. Flagged in previous review, still not addressed.
- **Suggested fix:** Add `# TBD — leave empty until the vendor employee RM role ID is confirmed` inline.
- **Comment for the dev:** "Same nit from the previous review: the empty `RM_SA_VENDOR_EMP_ROLE_ID` looks like a mistake without a comment. One line explaining it's TBD pending RM role confirmation is enough."

#### 4. Missing trailing newline in `.env.example`
- **File + line:** `.env.example` (last line)
- **Severity:** nit
- **What's wrong:** File still ends without a trailing newline. Flagged in previous review.
- **Suggested fix:** Add a blank line at the end.

#### 5. Two "owner" role ID config keys with no cross-reference
- **File + line:** `config/rent_manager.php:72` and `config/rent_manager.php:84`
- **Severity:** nit
- **What's wrong:** `owner_role_id` (RM_OWNER_ROLE_ID, default 70) and `sa_owner_role_id` (RM_SA_OWNER_ROLE_ID, default 808) coexist with different defaults and no explanation of their relationship. Neither has any callers yet.
- **Suggested fix:** Add an inline comment next to `owner_role_id` clarifying its purpose vs. `sa_owner_role_id`.

---

## Verdict
**Needs changes before merge** — 2 blockers: base migration still wrong (not addressed from v1 review), AWS vars accidentally removed during merge conflict resolution.

## SPACE log
```
PR review — SYM-197 (BE) — re-review
Time spent reviewing: ~30 min (parte de 1 hora total revisando sym-197 + sym-198 en conjunto)
Model: Sonnet (scoped/infrastructure change)
Findings: 2 blockers, 0 should-fix, 3 nits.
Verdict: needs changes before merge
```
